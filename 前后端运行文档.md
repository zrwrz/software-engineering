# 前后端运行文档

本文档用于在本机启动当前项目的后端（C++/oatpp）与前端（Vue/Vite），并完成基础联调。

## 1. 目录结构

- 后端：`code/backend`
- 前端：`code/frontend`
- 后端默认地址：`http://127.0.0.1:8081`
- 前端开发地址（默认）：`http://127.0.0.1:5173`

## 2. 启动前准备

### 2.1 数据库

项目默认使用 MySQL，默认连接参数（未设置环境变量时）：

- Host：`tcp://127.0.0.1:3306`
- User：`root`
- Password：空
- DB：`jiewu_project`

创建数据库（如尚未创建）：

```sql
CREATE DATABASE IF NOT EXISTS jiewu_project CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

导入表结构：

```bash
mysql -h 127.0.0.1 -P 3306 -u root jiewu_project < code/backend/sql/schema.sql
```

### 2.2 前端依赖

```bash
cd "/Users/gx/Documents/SE/code/frontend"
npm install
```

### 2.3 后端编译依赖

后端通过 CMake 构建，需具备：

- CMake
- C++17 编译器
- OpenSSL
- oatpp
- MySQL Connector/C++

## 3. 启动后端

打开终端 A：

```bash
cd "/Users/gx/Documents/SE/code/backend"
mkdir -p build
cd build
cmake ..
cmake --build . -j
```

设置运行环境变量并启动：

```bash
export JIEWU_DB_HOST="tcp://127.0.0.1:3306"
export JIEWU_DB_USER="root"
export JIEWU_DB_PASSWORD=""
export JIEWU_DB_NAME="jiewu_project"
export JIEWU_SERVER_PORT="8081"
export JIEWU_BIND_HOST="127.0.0.1"
export JIEWU_JWT_SECRET="dev-only-jwt-secret-change-me-2026"

./jiewu_server
```

启动成功标志（终端输出）：

- `Running on http://localhost:8081`

## 4. 启动前端

打开终端 B：

```bash
cd "/Users/gx/Documents/SE/code/frontend"
npm run dev
```

访问：

- `http://127.0.0.1:5173`

## 5. 前端联调配置

`code/frontend/.env.development` 推荐如下：

```env
VITE_API_BASE=/api/v1
VITE_API_PROXY_TARGET=http://localhost:8081
VITE_DEMO_MODE=false
VITE_SHOW_API_HINT=false
```

修改 `.env.development` 后，需要重启 `npm run dev` 才生效。

## 6. 最小联调检查

1. 打开前端登录页并登录。
2. 浏览器 Network 中请求应为 `http://127.0.0.1:5173/api/v1/...`（由 Vite 代理到后端）。
3. 后端终端可看到接口日志（如 `AuthController:[OK]`、`OrderController:[OK]`）。

## 7. 常见问题

### 7.1 前端提示网络错误/接口不可用

- 检查后端是否仍在运行。
- 检查 `VITE_API_PROXY_TARGET` 是否为 `http://localhost:8081`。
- 修改配置后是否重启了前端服务。

### 7.2 管理端接口连续失败（Token expired）

- 这是登录态过期，不是后端挂掉。
- 重新登录管理员账号即可恢复。
- 现已做前端过期提示：过期会跳登录页并提示“登录已过期，请重新登录”。

### 7.3 后端连不上数据库

- 检查 MySQL 服务状态。
- 检查 `JIEWU_DB_*` 环境变量。
- 确认库名是否为 `jiewu_project` 并已导入 schema。

## 8. 停止服务

- 前端终端按 `Ctrl + C`
- 后端终端按 `Ctrl + C`

